
sudo: required
env:
  - CONTAINER_ID=$(mktemp)

services:
  - docker

before_install:
  - sudo apt-get update
  # Pull containers
  - sudo docker pull centos:7
  # Customize containers
  - sudo docker build --rm=true --file=Dockerfile.centos --tag=centos:ansible .

script:
  # Run container in detached state
  - sudo docker run --detach --privileged --volume="${PWD}":/etc/ansible:ro --volume=/sys/fs/cgroup:/sys/fs/cgroup:ro centos:ansible /usr/lib/systemd/systemd > /tmp/container_id
  # Check syntax of ansible playbook
  - sudo docker exec "$(cat /tmp/container_id)" ansible-playbook -i localhost /etc/ansible/lightsiem-install.yml --syntax-check
  # Run ansible playbook
  - sudo docker exec "$(cat /tmp/container_id)" ansible-playbook /etc/ansible/lightsiem-install.yml --syntax-check
  # Clean up
  - sudo docker stop "$(cat /tmp/container_id)"


#notifications:
#  email: false
#
#
#
#
#sudo: required
#language: python
#python: "2.7"
#
#env:
#  - distribution: centos
#    version: 7
#    init: /usr/lib/systemd/systemd
#    run_opts: "--privileged --volume=/sys/fs/cgroup:/sys/fs/cgroup:ro"
#
#before_install:
#  - sudo apt-get update -qq
#
#install:
#  - pip install ansible
#  - "printf '[defaults]\nroles_path = ../' > ansible.cfg"
#
#script:
#  - ansible-playbook -i localhost lightsiem-install.yml --syntax-check